# RFLink Raw Tools v0.0.2 Log UI-only fix

This package keeps the existing Info / Configuration / Log app model and changes only the frontend log page polish plus cache-bust build id.

No backend files, services, YAML behavior, sidebar behavior, aliases, or switch entities are changed.

Changed files:

- custom_components/rflink_raw/www/rflink-raw-tools-panel.js
- custom_components/rflink_raw/const.py
