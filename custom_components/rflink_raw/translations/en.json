{
  "config": {
    "step": {
      "user": {
        "title": "RFLink Raw Tools",
        "description": "Create RFLink Raw Tools. Setup actions are buttons after setup; unavailable actions are disabled."
      }
    },
    "abort": {
      "already_configured": "RFLink Raw Tools is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "RFLink Raw Tools",
        "description": "No options here. Use the setup buttons on the device page or dashboard."
      }
    }
  }
}
